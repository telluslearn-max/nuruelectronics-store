export const SITE_URL = "https://www.nuruelectronics.com";

/**
 * Last time hand-authored content (categories/ecosystems/kits/buying guides)
 * materially changed — there's no CMS timestamp to hook into for these, so
 * this is bumped manually rather than faked. Used as the sitemap's
 * `lastModified` for every route that isn't a live Shopify product.
 */
export const CONTENT_REVISION_DATE = "2026-07-12";
